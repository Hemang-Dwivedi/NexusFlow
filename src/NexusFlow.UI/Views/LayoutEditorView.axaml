<UserControl xmlns="https://github.com/avaloniaui"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             x:Class="NexusFlow.UI.Views.LayoutEditorView">

  <Border Background="#111" Padding="16">
    <Border Background="#1E1E1E"
            BorderBrush="#444"
            BorderThickness="1"
            CornerRadius="10"
            Padding="12">

      <StackPanel Spacing="12">

        <!-- Command bar -->
        <StackPanel Orientation="Horizontal" Spacing="8">
          <Button Content="Revert"
                  Command="{Binding RevertCommand}"
                  IsEnabled="{Binding IsDirty}"
                  Width="90"/>

          <Button Content="Apply"
                  Command="{Binding ApplyCommand}"
                  IsEnabled="{Binding IsDirty}"
                  Width="90"/>
        </StackPanel>

        <!-- Drag surface -->
        <Border x:Name="PeerDragSurface"
                Background="#202020"
                BorderBrush="#555"
                BorderThickness="1"
                CornerRadius="10"
                Padding="12">

          <DockPanel LastChildFill="True">

            <TextBlock DockPanel.Dock="Top"
                       Foreground="White"
                       FontSize="18"
                       Text="{Binding LocalCluster.PeerName}" />

            <Button DockPanel.Dock="Bottom"
                    HorizontalAlignment="Right"
                    Width="90"
                    Content="Identify" />

            <!-- Layout canvas -->
            <Border Margin="0,10,0,10"
                    Background="#0B0B0B"
                    BorderBrush="#333"
                    BorderThickness="1"
                    CornerRadius="8"
                    Padding="8">

              <!-- THIS moves (draft offsets) -->
              <Border x:Name="PeerClusterHost"
                      Background="Transparent">
                <Border.RenderTransform>
                  <TranslateTransform X="{Binding DraftOffsetX}"
                                      Y="{Binding DraftOffsetY}"/>
                </Border.RenderTransform>

                <!-- Viewport -->
                <Border Width="{Binding CanvasWidth}"
                        Height="{Binding CanvasHeight}"
                        HorizontalAlignment="Left"
                        VerticalAlignment="Top"
                        Background="Transparent">

                  <ItemsControl Width="{Binding CanvasWidth}"
                                Height="{Binding CanvasHeight}"
                                ItemsSource="{Binding Normalized.Displays}"
                                HorizontalAlignment="Stretch"
                                VerticalAlignment="Stretch">

                    <ItemsControl.ItemsPanel>
                      <ItemsPanelTemplate>
                        <Canvas Width="{Binding CanvasWidth}"
                                Height="{Binding CanvasHeight}"/>
                      </ItemsPanelTemplate>
                    </ItemsControl.ItemsPanel>

                    <ItemsControl.ItemTemplate>
                      <DataTemplate>
                        <Border Width="{Binding Nw}"
                                Height="{Binding Nh}"
                                Background="#0078D4"
                                BorderBrush="White"
                                BorderThickness="1"
                                CornerRadius="6">
                          <Border.RenderTransform>
                            <TranslateTransform X="{Binding Nx}"
                                                Y="{Binding Ny}" />
                          </Border.RenderTransform>

                          <TextBlock Text="{Binding Source.DisplayNumber}"
                                     Foreground="White"
                                     FontSize="28"
                                     HorizontalAlignment="Center"
                                     VerticalAlignment="Center"/>
                        </Border>
                      </DataTemplate>
                    </ItemsControl.ItemTemplate>

                  </ItemsControl>

                </Border>
              </Border>

            </Border>

          </DockPanel>
        </Border>

      </StackPanel>
    </Border>
  </Border>
</UserControl>
